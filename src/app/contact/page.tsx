import ContactSection from "@/components/sections/contact";

export const metadata = {
  title: "Contact | Vikrant Dey",
  description:
    "Get in touch with Vikrant Dey for internships, startup roles, collaborations, and technically serious product work.",
};

export default function ContactPage() {
  return <ContactSection />;
}
